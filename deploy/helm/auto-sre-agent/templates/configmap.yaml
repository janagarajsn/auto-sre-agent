apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "auto-sre-agent.fullname" . }}-config
data:
  ENV: {{ .Values.env.ENV | quote }}
  LOG_LEVEL: {{ .Values.env.LOG_LEVEL | quote }}
  REDIS_URL: {{ .Values.redis.url | quote }}
  PROMETHEUS_URL: {{ .Values.prometheus.url | quote }}
