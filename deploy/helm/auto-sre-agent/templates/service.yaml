apiVersion: v1
kind: Service
metadata:
  name: {{ include "auto-sre-agent.fullname" . }}
  labels:
    {{- include "auto-sre-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "auto-sre-agent.selectorLabels" . | nindent 4 }}
